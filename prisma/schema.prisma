generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Product {
  id          String @id
  name        String
  price       Int
  imageUrl    String @map("image_url")
  description String

  @@map("products")
}
