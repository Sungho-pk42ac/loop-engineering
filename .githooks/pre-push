#!/bin/sh
# 로컬 CI: push 직전에 린트 + 테스트를 실행한다. 실패하면 push가 중단된다.
# GitHub Actions와 동일한 검사 (.github/workflows/ci.yml 5~6번 스텝).
# 급할 때 건너뛰기: git push --no-verify
set -e
echo "[pre-push] pnpm lint"
pnpm lint
echo "[pre-push] pnpm exec vitest run"
pnpm exec vitest run
